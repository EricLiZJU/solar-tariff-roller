# 中财管道光伏项目 IRR 反推电价计算逻辑说明

## 1. 计算目标

本计算脚本用于对光伏项目进行 IRR 反推电价测算。其核心目标是在给定目标内部收益率（IRR）的情况下，反向计算项目所需的自用电折后价，使项目在重新生成月度现金流后，测算得到的年化 IRR 达到目标值。

本脚本当前设置的目标 IRR 为：

```python
TARGET_IRR = 0.09
```

即目标内部收益率为 **9%**。

整体计算逻辑可以概括为：

```text
读取 Excel 基础数据
↓
获取当前电价、上网电价、消纳率、投资额、运维费等参数
↓
读取已有自用电费、上网电费、历史含税收入和历史现金流
↓
给定一个候选自用电折后价
↓
重新计算月度含税收入、税费、成本和现金流
↓
根据月度现金流计算 IRR
↓
通过二分法不断调整候选电价
↓
找到使 IRR 达到目标值的目标电价
```

---

## 2. 数据来源

程序从 Excel 文件中读取两个工作表：

```python
ws = wb['项目基础数据']
ws2 = wb['月滚动现金流量表']
```

其中：

| 工作表 | 作用 |
|---|---|
| 项目基础数据 | 读取当前自用电价、上网电价、自用消纳率、年运维费、自用电费、上网电费等基础数据 |
| 月滚动现金流量表 | 读取总投资、历史含税收入、历史月度现金流等滚动测算数据 |

---

## 3. 主要输入参数

### 3.1 当前自用电折后价 O_old

```python
O_old = ws['D26'].value
```

该指标表示当前 Excel 测算表中已经设定的自用电折后价，单位通常为：

```text
元/kWh
```

它代表企业消纳光伏电时，当前实际结算的电价。

程序后续会基于该旧电价，反推出新的目标电价 `O_new`。

---

### 3.2 上网电价 grid_price

```python
grid_price = ws['D23'].value
```

上网电价表示光伏电未被企业消纳、出售给电网时的结算价格。

其计算逻辑为：

```text
上网电费 = 上网电量 × 上网电价
```

在本脚本中，上网电价保持不变，反推过程中主要调整的是自用电折后价。

---

### 3.3 自用消纳率 self_use_ratio

```python
self_use_ratio = ws['C15'].value
```

自用消纳率表示光伏总发电量中由企业直接使用的比例。

例如：

```text
自用消纳率 = 85%
```

表示：

```text
85% 的发电量由企业自用
15% 的发电量上网出售
```

自用消纳率会影响项目收入结构，因为自用电量和上网电量分别按照不同价格结算。

---

### 3.4 总投资 investment

```python
investment = ws2['F6'].value
```

总投资从“月滚动现金流量表”的 `F6` 单元格读取，单位通常为万元。

在现金流计算中，总投资作为第 0 期现金流：

```text
CF0 = -总投资
```

即项目开始时先发生投资支出，因此为负值。

---

### 3.5 年运维费 om_annual

```python
om_annual = ws['G10'].value
```

年运维费表示项目每年发生的运维成本。程序中将其拆分为半年度运维费：

```python
om_half = om_annual / 2
```

即假设运维费每半年发生一次。

---

## 4. 自用电费和上网电费读取逻辑

程序从“项目基础数据”表中读取 P 列和 M 列：

```python
for r in range(63, 88):
    p = ws.cell(r, 16).value  # P column
    m = ws.cell(r, 13).value  # M column
```

其中：

| 指标 | 含义 |
|---|---|
| P 列 | 自用电费 |
| M 列 | 上网电费 |

读取范围为第 63 行到第 87 行，共 25 行，对应项目第 1 年至第 25 年。

也就是说，程序读取的是：

```text
第1年自用电费、第1年上网电费
第2年自用电费、第2年上网电费
……
第25年自用电费、第25年上网电费
```

后续程序会根据新的自用电折后价，重新调整自用电费，并重新生成年度总收入。

---

## 5. 历史含税收入 baseline_c

程序读取“月滚动现金流量表”中的 `C7:C50`：

```python
for r in range(7, 51):
    c = ws2.cell(r, 3).value
```

该区间对应前 44 个月的含税收入。

```text
C7:C50 = 第1个月至第44个月含税收入
```

这部分收入在脚本中被视为历史基准数据或已形成的滚动测算数据。

需要注意的是，本脚本并不是直接固定这 44 个月的含税收入，而是会根据新旧综合电价比例进行缩放：

```python
monthly_c.append(c * ratio)
```

因此，这里的业务假设是：

```text
前 44 个月含税收入可以随新旧综合电价比例进行同比例调整
```

如果前 44 个月是真实已发生收入，则理论上不应再缩放；如果前 44 个月是旧电价下的测算收入，则按比例缩放是合理的。该口径需要在实际项目中与客户确认。

---

## 6. 历史月度现金流 baseline_m

程序读取“月滚动现金流量表”中的 M 列：

```python
for r in range(6, 307):
    m = ws2.cell(r, 13).value
```

该数据用于计算当前 Excel 方案下的 IRR：

```python
irr_current = npf.irr(baseline_m) * 12
```

也就是说：

```text
当前 IRR = 根据 Excel 原有月度现金流计算得到的 IRR
```

该值用于展示当前测算方案下项目收益率水平。

---

## 7. 新旧综合电价比例 ratio

在反推过程中，程序每次会尝试一个新的自用电折后价 `O_new`。

为了判断新电价相对旧电价对收入的影响，程序先计算新旧综合电价：

```python
k_old = self_use_ratio * O_old + (1 - self_use_ratio) * grid_price
k_new = self_use_ratio * O_new + (1 - self_use_ratio) * grid_price
ratio = k_new / k_old
```

旧综合电价为：

```text
旧综合电价 = 自用消纳率 × 旧自用电价 + (1 - 自用消纳率) × 上网电价
```

新综合电价为：

```text
新综合电价 = 自用消纳率 × 新自用电价 + (1 - 自用消纳率) × 上网电价
```

新旧综合电价比例为：

```text
ratio = 新综合电价 / 旧综合电价
```

该比例用于调整前 44 个月的含税收入：

```text
新含税收入 = 原含税收入 × ratio
```

---

## 8. 年度收入 q_new 的重新计算

程序读取了原 Excel 中的自用电费 `P` 和上网电费 `M`。当自用电折后价发生变化时，上网电费保持不变，自用电费按新旧电价比例调整。

计算逻辑如下：

```python
p_new = p_values[i] * (O_new / O_old)
q_new.append(m_values[i] + p_new)
```

对应公式为：

```text
新自用电费 = 原自用电费 × 新自用电价 / 原自用电价
```

```text
新年度总收入 = 上网电费 + 新自用电费
```

即：

```text
Q_new = M + P_new
```

其中：

| 指标 | 含义 |
|---|---|
| P_new | 新电价下的自用电费 |
| M | 上网电费，保持不变 |
| Q_new | 新电价下的年度总收入 |

---

## 9. 月度含税收入 monthly_c 的构建

项目周期为 25 年，即：

```text
25 年 × 12 个月 = 300 个月
```

程序需要构造 300 个月的含税收入序列。

### 9.1 第 1-44 个月

前 44 个月使用 Excel 中已有的含税收入基准值，并按新旧综合电价比例调整：

```python
for c in baseline_c:
    monthly_c.append(c * ratio)
```

即：

```text
第1-44个月含税收入 = 原含税收入 × ratio
```

### 9.2 第 45-48 个月

第 45-48 个月使用第 1 年年度收入平均分摊：

```python
for _ in range(4):
    monthly_c.append(q_new[0] / 12)
```

即：

```text
第45-48个月含税收入 = 第1年年度总收入 / 12
```

这里之所以只有 4 个月，是因为滚动表可能已经位于第 1 年后期，前 44 个月已经由历史数据覆盖。

### 9.3 后续月份

第 2 年至第 25 年按照每年 12 个月进行平均分摊。程序中根据不同年份下标，将每一年的年度收入 `q_new` 平均分配到 12 个月。

整体上，程序把年度收入转换为月度含税收入，形成完整的 300 个月收入序列。

---

## 10. 成本费用计算逻辑

### 10.1 保险费

保险费按总投资的千分之一计算：

```python
insurance = investment * 0.001
```

即：

```text
保险费 = 总投资 × 0.001
```

程序设定每年第一个月发生保险费：

```python
g_m = insurance if (month_num % 12 == 1) else 0
```

即第 1、13、25、37 等月份发生保险费。

---

### 10.2 运维费

年运维费拆分为半年度运维费：

```python
om_half = om_annual / 2
```

即：

```text
半年度运维费 = 年运维费 / 2
```

程序设定每 6 个月发生一次运维费：

```python
h_m = om_half if (month_num % 6 == 1) else 0
```

即第 1、7、13、19 等月份发生运维费。

---

### 10.3 当月成本费用

```python
i_total = g_m + h_m
```

即：

```text
当月成本费用 = 保险费 + 运维费
```

本脚本中，月度成本费用主要包括保险费和运维费。

---

## 11. 税费计算逻辑

### 11.1 初始进项税 I0

```python
I0 = round((investment * 0.7 / 1.13 * 0.13 + investment * 0.3 / 1.09 * 0.09), 2)
```

该公式假设总投资由两部分构成：

| 投资类别 | 占比 | 税率 |
|---|---:|---:|
| 设备类投资 | 70% | 13% |
| 建筑安装类投资 | 30% | 9% |

因此，初始进项税为：

```text
初始进项税 =
总投资 × 70% ÷ 1.13 × 13%
+
总投资 × 30% ÷ 1.09 × 9%
```

程序将该初始进项税作为可抵扣税额进行结转：

```python
k_carry = -I0
```

这里用负数表示未来仍有可抵扣的进项税。

---

### 11.2 成本费用进项税 j_m

```python
j_m = round(i_total / 1.06 * 0.06, 2) if i_total > 0 else 0
```

该公式假设运维费、保险费等成本费用适用 6% 税率。

```text
进项税金 = 成本费用 ÷ 1.06 × 6%
```

当月没有成本费用时，进项税金为 0。

---

### 11.3 收入销项税 e_m

```python
d_m = round(c / 1.13, 2)
e_m = round(d_m * 0.13, 2)
```

其中：

| 指标 | 含义 |
|---|---|
| c | 当月含税收入 |
| d_m | 当月不含税收入 |
| e_m | 当月销项税金 |

计算逻辑为：

```text
不含税收入 = 含税收入 ÷ 1.13
销项税金 = 不含税收入 × 13%
```

也可以理解为：

```text
销项税金 = 含税收入 ÷ 1.13 × 13%
```

---

### 11.4 应缴增值税 k_m

```python
k_m = e_m - j_m + k_carry
```

计算逻辑为：

```text
应缴增值税计算值 = 销项税金 - 当月进项税金 + 前期结转
```

因为 `k_carry` 可能为负数，所以它可以抵减当月销项税。

当 `k_m < 0` 时，表示进项税尚未抵扣完，当月无需实际缴纳增值税，负数继续结转到下月。

当 `k_m > 0` 时，表示前期进项税已经抵扣完，当月需要实际缴纳增值税。

---

### 11.5 增值税结转 k_carry

```python
k_carry = min(k_m, 0) if k_m < 0 else 0
```

其含义是：

```text
如果 k_m 为负数，则继续结转该负数
如果 k_m 为正数，则说明需要缴税，结转余额清零
```

即：

```text
k_carry = k_m，若 k_m < 0
k_carry = 0，若 k_m >= 0
```

---

### 11.6 教育费及附加 l_m

```python
l_m = round(k_m * 0.12, 2) if k_m > 0 else 0
```

教育费及附加只在应缴增值税为正时计算：

```text
教育费及附加 = 应缴增值税 × 12%
```

如果当月无需实际缴纳增值税，则教育费及附加为 0。

---

## 12. 当月现金流 m_cf

当月现金流计算公式为：

```python
m_cf = c - i_total - (k_m if k_m > 0 else 0) - l_m
```

即：

```text
当月现金流 =
含税收入
-
成本费用
-
实际缴纳增值税
-
教育费及附加
```

其中：

```python
(k_m if k_m > 0 else 0)
```

表示只有当 `k_m` 为正，即需要实际缴纳增值税时，才从现金流中扣除增值税。

若 `k_m` 为负，则表示进项税继续结转，并不形成当月现金流支出。

---

## 13. IRR 计算逻辑

现金流序列从第 0 期开始：

```python
cashflows.append(-investment)
```

第 0 期现金流为：

```text
CF0 = -总投资
```

后续 300 个月，每个月加入对应的月度现金流：

```python
cashflows.append(m_cf)
```

最终形成如下现金流结构：

```text
第0期：-总投资
第1个月：月度现金流
第2个月：月度现金流
……
第300个月：月度现金流
```

然后使用 `numpy_financial.irr` 计算月度 IRR：

```python
npf.irr(cashflows)
```

程序将月度 IRR 乘以 12，得到简单年化 IRR：

```python
return npf.irr(cashflows) * 12
```

即：

```text
年化 IRR = 月度 IRR × 12
```

需要注意的是，严格的复利年化 IRR 通常为：

```text
年化 IRR = (1 + 月度 IRR)^12 - 1
```

而本脚本采用的是简单年化方式。实际项目中是否需要改为复利年化，应以客户确认的测算口径为准。

---

## 14. 二分法反推目标电价

函数 `binary_search` 用于在给定区间内寻找目标电价：

```python
def binary_search(..., target_irr, lo=0.3, hi=0.9):
```

搜索范围默认为：

```text
0.30 元/kWh 至 0.90 元/kWh
```

二分法逻辑如下：

```text
1. 取 lo 和 hi 的中间值 mid
2. 用 mid 作为候选自用电折后价重新计算 IRR
3. 如果计算 IRR 高于目标 IRR，说明电价偏高，将 hi 调整为 mid
4. 如果计算 IRR 低于目标 IRR，说明电价偏低，将 lo 调整为 mid
5. 重复上述过程，直到结果收敛
```

其基本原理是：

```text
电价越高 → 收入越高 → 现金流越好 → IRR 越高
电价越低 → 收入越低 → 现金流越差 → IRR 越低
```

因此，可以通过不断缩小价格区间，找到使 IRR 接近目标值的电价。

---

## 15. 最终输出结果说明

### 15.1 当前 IRR

```python
irr_current = npf.irr(baseline_m) * 12
```

表示 Excel 当前现金流方案对应的年化 IRR。

---

### 15.2 目标电价 O66-O87

```python
目标电价 O66-O87 = O_result
```

表示程序通过二分法反推出的自用电折后价。

该价格用于填入 Excel 项目基础数据表中的 `O66:O87` 单元格，使项目 IRR 达到目标值。

---

### 15.3 对应 D24

```python
对应 D24 = O_result / 0.85
```

该计算说明 `O66:O87` 与 `D24` 之间存在 85 折关系：

```text
O66:O87 = D24 × 0.85
```

因此：

```text
D24 = O_result ÷ 0.85
```

通常可理解为：

| 指标 | 含义 |
|---|---|
| O66:O87 | 折后自用电价 |
| D24 | 折前综合电价或基准电价 |
| 0.85 | 折扣系数 |

---

### 15.4 验证 IRR

程序会将反推出的目标电价重新代入现金流模型，并再次计算 IRR：

```python
irr_result = compute_irr(O_result, ...)
```

如果验证 IRR 接近目标 IRR，则说明反推结果有效。

---

## 16. 本脚本的主要特点

本脚本与较早版本相比，有以下特点：

1. **总投资从 Excel 中读取**  
   总投资由 `ws2['F6']` 读取，而不是在代码中固定写死，因此更适合不同项目复用。

2. **年运维费从 Excel 中读取**  
   年运维费由 `ws['G10']` 读取，并在程序中按半年进行摊分。

3. **前 44 个月含税收入按比例缩放**  
   程序对 `C7:C50` 的历史含税收入按新旧综合电价比例进行调整。该逻辑是否合理，需要结合该部分数据是否为真实已发生收入进行确认。

4. **通过 P 列和 M 列重新构造年度收入**  
   程序不直接根据年度发电量重新计算收入，而是基于 Excel 中已有的自用电费 P 列和上网电费 M 列进行调整，因此更容易与原 Excel 测算表口径保持一致。

5. **使用二分法反推电价**  
   程序在 0.30 至 0.90 元/kWh 之间搜索目标电价，使计算 IRR 接近目标 IRR。

---

## 17. 关键注意事项

### 17.1 前 44 个月收入是否应缩放

当前脚本对前 44 个月含税收入使用以下逻辑：

```python
monthly_c.append(c * ratio)
```

如果这 44 个月是历史真实收入，原则上应保持固定，不应随新电价变化。

如果这 44 个月是旧电价下的测算收入，则按新旧综合电价比例缩放是合理的。

因此，该逻辑应在正式使用前与客户确认。

---

### 17.2 IRR 年化方式

当前脚本使用：

```python
年化 IRR = 月度 IRR × 12
```

该方式为简单年化。

如需采用复利年化，则应改为：

```python
年化 IRR = (1 + 月度 IRR) ** 12 - 1
```

最终采用哪种方式，应以原 Excel 测算口径或客户确认口径为准。

---

### 17.3 税费口径

当前脚本假设：

| 项目 | 税率 |
|---|---:|
| 电费收入销项税 | 13% |
| 运维费和保险费进项税 | 6% |
| 设备类投资进项税 | 13% |
| 建筑安装类投资进项税 | 9% |
| 教育费及附加 | 应缴增值税的 12% |

以上税费规则应与客户原 Excel 表、项目实际情况及税务口径保持一致。

---

## 18. 总结

本脚本的核心逻辑是：读取 Excel 中已有的项目基础数据、月度滚动现金流和收入数据，在给定目标 IRR 的前提下，通过调整自用电折后价，重新生成项目 300 个月的月度现金流，并计算对应 IRR。

程序使用二分法不断试算新的自用电折后价，最终找到使项目年化 IRR 达到目标值的电价。该结果可用于指导 Excel 表中 `O66:O87` 单元格的填写，并通过 `D24 = O_result / 0.85` 得到对应的折前综合电价。

简单来说，本脚本完成的是：

```text
目标 IRR → 反推自用电折后价 → 重新生成现金流 → 校验 IRR
```

其中，最核心的输出结果是：

```text
目标电价 O66-O87
```

它表示在当前项目参数、成本费用、税费规则和滚动现金流口径下，为达到目标 IRR 所需的自用电折后价。
```
